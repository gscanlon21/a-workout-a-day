<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Hybrid.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Hybrid"
    x:DataType ="local:AppShellViewModel"
    Shell.FlyoutBehavior="Flyout"
    Shell.FlyoutHeaderBehavior="CollapseOnScroll">


    <FlyoutItem Title="Daily Workout" FlyoutIcon="{StaticResource IconOne}">
        <ShellContent Title="Daily Workout"
                      Route="NewsletterPage"
                      ContentTemplate="{DataTemplate local:NewsletterPage}" />
    </FlyoutItem>

    <MenuItem Text="Source"
              IconImageSource="{StaticResource IconTwo}"
              Command="{Binding SourceCommand}"
              CommandParameter="{Binding SiteSettings.Value.Source}" />

    <MenuItem Text="Logout"
              IconImageSource="{StaticResource IconThree}"
              Command="{Binding LogoutCommand}" />
</Shell>